# 🏗️ هيكلية نظام MindTab

## نظرة عامة
MindTab هو **إضافة Chrome تركز على الخصوصية أولاً**، مبنية باستخدام تقنيات React الحديثة. تستفيد من واجهة برمجة تطبيقات Chrome Extensions Manifest V3 وتجربة `window.ai` (Gemini Nano) للذكاء الاصطناعي على الجهاز.

---

## 🧩 المكونات الأساسية

### 1. العقل (Background Service Worker)
*   **الدور**: المنسق المركزي. يعمل دائماً (أو يستيقظ عند الحاجة).
*   **المسؤوليات**:
    *   إدارة حالة علامات التبويب والنوافذ عبر `chrome.tabs` و `chrome.windows`.
    *   معالجة الأوامر العامة (اختصارات لوحة المفاتيح).
    *   تنسيق الاتصال بين سياقات المتصفح المختلفة.

### 2. الواجهة (Side Panel & Popup)
*   **التقنيات**: React 19, TailwindCSS, Zustand.
*   **الدور**: طبقة تفاعل المستخدم.
*   **الميزات الرئيسية**:
    *   **ChatPanel**: واجهة دردشة فورية مع استجابات متدفقة.
    *   **TabList**: تمثيل مرئي للمجموعات.

### 3. الذكاء (LLM Service)
*   **نمط المحول (Adapter Pattern)**: مصمم لدعم جهات خلفية متعددة، مع التركيز على الذكاء الاصطناعي المحلي.
*   **ChromeAIAdapter**:
    *   يتواجه مع `window.ai` / `ai.languageModel`.
    *   يعالج قيود هندسة الأوامر (نوافذ السياق، ضبط الأوامر).
    *   **حدود الخصوصية**: يضمن أن البيانات الحساسة (العناوين/الروابط) لا تغادر سياق `chrome.scripting` إلى خادم خارجي.

---

## 📊 مخطط تدفق البيانات

```mermaid
graph TD
    User[تفاعل المستخدم] --> UI[واجهة React الجانبية]
    UI --> Store[مخزن حالة Zustand]
    
    subgraph "سياق إضافة Chrome"
        Store --> Service[طبقة الخدمات]
        Service --> TabsService[خدمة التبويبات]
        Service --> LLMService[خدمة الذكاء الاصطناعي]
        
        TabsService --> ChromeAPI[واجهات Chrome]
        ChromeAPI --> Browser[نوافذ/تبويبات المتصفح]
        
        LLMService --> Adapter[محول ChromeAI]
        Adapter --> LocalModel[Gemini Nano (محلي)]
    end
    
    Browser -- "يقرأ المحتوى" --> ContentScript[سكربت المحتوى]
    ContentScript -- "نص منقح" --> Service
```

## 🔐 تصميم الأمن والخصوصية
1.  **التنفيذ المحلي**: `ChromeAIAdapter` هو الافتراضي. لا يتم إجراء أي طلبات شبكة لـ OpenAI/Anthropic إلا إذا قام المستخدم بتهيئة ذلك صراحة (وهو معطل حالياً في هذه النسخة).
2.  **تطوي/تنقيح المحتوى**:
    *   **الروابط**: يتم تجريد معلمات الاستعلام (التي غالباً ما تحتوي على رموز وصول) قبل إرسالها للنموذج.
    *   **العناوين**: يتم قصها لمنع تجاوز سياق الذاكرة وتقليل التسرب.
3.  **الأذونات**:
    *   `activeTab`: يتم طلبها فقط عندما يتصرف المستخدم في علامة تبويب محددة.
    *   `scripting`: تستخدم بصرامة لاستخراج نص DOM (للقراءة فقط).

---

## 📂 هيكل المشروع
```text
src/
├── background/      # نقطة دخول Service Worker
├── components/      # مكونات واجهة React
├── services/        # منطق العمل
│   ├── llm/         # محولات الذكاء الاصطناعي
│   └── tabs/        # منطق إدارة التبويبات
├── stores/          # إدارة الحالة (Zustand)
├── styles/          # Tailwind والتنسيقات العامة
└── types/           # تعريفات TypeScript
```
